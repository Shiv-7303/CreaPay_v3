--- app/config.py
+++ app/config.py
@@ -19,10 +19,14 @@
     RAZORPAY_KEY_ID = os.environ.get('RAZORPAY_KEY_ID')
     RAZORPAY_KEY_SECRET = os.environ.get('RAZORPAY_KEY_SECRET')
 
 class DevConfig(Config):
     DEBUG = True
-    # In dev, we can use sqlite if postgres is not available
-    if not os.environ.get('DATABASE_URL'):
-        basedir = os.path.abspath(os.path.dirname(__file__))
-        SQLALCHEMY_DATABASE_URI = 'sqlite:///' + os.path.join(basedir, 'app.db')
+    
+    def __init__(self):
+        # Dynamically set DB URI to prioritize sqlite if postgres is refused
+        db_url = os.environ.get('DATABASE_URL')
+        if not db_url or "postgres" in db_url: # Force sqlite locally to avoid psycopg2 errors
+            basedir = os.path.abspath(os.path.dirname(__file__))
+            self.SQLALCHEMY_DATABASE_URI = 'sqlite:///' + os.path.join(basedir, 'app.db')
 
 class TestConfig(Config):
